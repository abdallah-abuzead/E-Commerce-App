import 'package:flutter/material.dart';

class EditBrandDesktop extends StatelessWidget {
  const EditBrandDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
